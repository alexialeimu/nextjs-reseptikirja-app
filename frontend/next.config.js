/** @type {import('next').NextConfig} */
const nextConfig = {
    // strict mode false, in order for elements triggered by subscription to not render twice
    reactStrictMode: false,
};

module.exports = nextConfig;
